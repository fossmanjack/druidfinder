import { Container, Row, Col } from 'reactstrap';

export default function GroveCard({ props }) {
	const {
		name,
		id,
		address,
		city,
		region,
		country,
		postCode,
		founded,
		membership,
		affiliation,
		email,
		web,
		phone,
		officers,
		coords,
		updated
	} = props;

	return (

	);
}

